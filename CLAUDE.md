# このリポジトリ

| ディレクトリ | 内容 |
|---|---|
| `agent/` | 業務エージェント（予定・タスク・案件・書類・SNS・PC点検） |
| `kintai/` | 現場勤怠・人件費管理アプリ |
| `shokunin/` | 工務店間 職人シェアアプリ「匠コネクト」 |
| `gyousha/` | 工務店×協力業者「匠協コネクト」（管理=admin.html／利用者=index.html。協力業者の名簿管理＋作業依頼＋一斉お知らせ＋返信。Firebase共有＋招待リンク方式、体験版 ?demo=1 あり） |
| `pc-care/` | PC点検・最適化スクリプト |
| `daiku/` | 大工パズル「上棟バランス」（骨組み組み立てバランスゲーム） |
| `marubatsu/` | 〇×ゲーム「消える〇×」（印は3つまで＝4つ目で最古が消える＝引き分けなし。CPU5段階（lv1れんしゅう〜lv5おに＝先読み1/2/4/6/8手）／ふたり対戦／オンライン対戦。勝負は3勝先取の5回勝負。PWA対応。オンラインはルートのオセロと同じFirebaseを流用し `rooms/mb-<合言葉4文字>` を使う＝DBルール変更不要） |
| `kikansha/` | しりとりゲーム「しりとり機関車」（線路を流れてくる貨車の中から、しりとりでつながる語をタップして連結。**石炭ゲージが減り続け、連結すると補給＝時間制限**（長い語ほど多く入る）。**10両ごとに駅にとうちゃく＝日本地図を札幌→那覇と南下**（石炭満タン＋ボーナス）。「ン」で終わる語をつなぐと脱線＝ワナ。のんびり/ふつう/とっきゅうの3段階、連結するほど加速。語句は `words.js` に1行ずつ足すだけ＝行き止まりの語は読み込み時に自動で除外。PWA） |
| ルート | 4人対戦オセロ |

技術：バニラ HTML/CSS/JS（ビルド不要）、PWA、ローカルは `serve.ps1`。

## 業務エージェントの作業をするとき

**必ず `agent/AGENT.md` を最初に読むこと。** あれが最上位ルール。要点：

- **決済・送金・入出金は絶対に実行しない。**判断も実行もオーナー本人が行う
- `agent/data/` は施主の個人情報・金額を含む。**外部に一切出さない**（Git 対象外）
- 見積・契約・請求は**下書きまで**。SNS は**下書きとコンプラ判定まで**。発行・投稿は人
- 有料 API / 有料サービスは使わない（必要なら費用を示して事前承認）
